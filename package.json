{
  "scripts": {
    "start": "parcel serve --no-hmr index.html",
    "build": "rm -rf docs && parcel build --no-optimize --no-scope-hoist --no-source-maps --public-url /recover-invalid-mnemonic --dist-dir docs index.html"
  },
  "dependencies": {
    "bip39": "^3.0.4",
    "js-levenshtein": "^1.1.6"
  },
  "devDependencies": {
    "buffer": "^5.5.0||^6.0.0",
    "events": "^3.1.0",
    "parcel": "^2.10.1",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0"
  }
}
